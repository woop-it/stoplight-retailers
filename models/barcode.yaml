type: object
x-examples:
  Example 1:
    value: 54fd56g4f5gfd456sd
    type: barcode
    format: '128'
title: barcode references (barcodeRef)
description: Bar code / QRCode of the parcel
required:
  - value
  - type
  - format
properties:
  value:
    type: string
    description: Barcode value
  type:
    description: 'Barcode format (128) '
    enum:
      - barcode
  format:
    description: Barcode display type
    enum:
      - '128'
